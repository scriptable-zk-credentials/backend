use sea_orm::Database;

pub async fn db_start() {
    //let db = Database::connect("sqlite://path/to/db.sqlite?mode=rwc").await
    //    .expect("failed to connect to create DB connection");
    //assert!(db.ping().await.is_ok());
    println!("TODO: open DB connection")
}